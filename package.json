{
  "name": "jev2mcp",
  "version": "0.2.1",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node src/server.mjs",
    "test": "node --test test/*.test.mjs",
    "eval": "node scripts/evaluate.mjs",
    "format": "prettier --write src extension public test scripts/*.mjs README.md SECURITY.md docs/design.md package.json docs/ci-example.yml"
  },
  "engines": {
    "node": ">=22.13"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "prettier": "^3.9.8"
  },
  "license": "MIT",
  "description": "Context-aware MCP, plugin and tool routing for LLMs.",
  "repository": {
    "type": "git",
    "url": "https://github.com/n8mirai/jev2mcp.git"
  }
}
